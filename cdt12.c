#include<stdio.h>
#include<conio.h>
void main()
{
int num,orginalnum,reverse;
printf ("enter the number check:");
scanf("%d",&num);
whlie(num!=0)
{
reverse=reverse*10+num%10;
num/=10;
}
printf ("/n the number is a palidrome");
{
else
{
printf ("%n its not palidrome");
}
return(0);
}
